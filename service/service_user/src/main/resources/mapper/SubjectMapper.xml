<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.studyweb.mapper.SubjectMapper">
   <!-- <resultMap id="getSubjectAndCourse" type="com.rabbit.model.pojo.dto.HomeMenusDTO">
        <id property="id" column="id"></id>
        <result property="title" column="title"></result>
        <collection property="courseList" ofType="com.rabbit.model.pojo.Course">
            <id property="id" column="c_id"></id>
            <result property="title" column="c_title"></result>
            <result property="cover" column="c_cover"></result>
            <result property="subTitle" column="c_sub_title"></result>
            <result property="buyCount" column="c_buy_count"></result>
            <result property="description" column="c_description"></result>
            <result property="price" column="c_price"></result>
        </collection>
    </resultMap>
    <select id="getSubjectAndCourse" resultMap="getSubjectAndCourse">
        select s.id ,s.title title,
               c.id c_id,c.title c_title,c.cover c_cover,c.sub_title c_sub_title,
               c.buy_count c_buy_count ,c.description c_description,c.price c_price
        from sys_course c right join sys_subject s
        on s.id=c.subject_pid
        where c.`status`=1
        order by  s.id
    </select>-->
</mapper>
